# Hustle Revenue Intelligence Deployment

Deploy the frontend to Cloudflare Pages, run the FastAPI backend behind a Cloudflare-managed origin, and configure the product with `NEXT_PUBLIC_API_BASE_URL` plus Cloudflare API credentials in environment variables.

@BryteSikaStrategyAI
