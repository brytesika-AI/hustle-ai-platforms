# Hustle CX Intelligence Deployment

Deploy the Next.js frontend to Cloudflare Pages, containerize the FastAPI backend, and expose the backend through Cloudflare Tunnel. Set `NEXT_PUBLIC_API_BASE_URL` along with Cloudflare account and token secrets in the deployment environment.

@BryteSikaStrategyAI
