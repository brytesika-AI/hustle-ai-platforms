# Hustle Fraud Intelligence Deployment

Deploy the frontend to Cloudflare Pages, containerize the backend with the included Dockerfile, and place the backend behind Cloudflare Tunnel. Configure `NEXT_PUBLIC_API_BASE_URL` and Cloudflare API credentials via deployment secrets.

@BryteSikaStrategyAI
