# Hustle Workforce Backend

Python backend for analytics, AI logic, upload handling, knowledge operations, and executive briefing generation.

Run locally with:

```bash
pip install -r backend/requirements.txt
uvicorn backend.app.main:app --reload --host 127.0.0.1 --port 8000
```

@BryteSikaStrategyAI
