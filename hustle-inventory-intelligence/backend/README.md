# Hustle Inventory Intelligence Backend

Python backend for stock visibility analytics, reorder guidance, slow-moving inventory detection, supplier dependency review, and executive inventory brief generation.

Run locally with:

```bash
pip install -r backend/requirements.txt
uvicorn backend.app.main:app --reload --host 127.0.0.1 --port 8000
```

@BryteSikaStrategyAI
